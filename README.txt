This is a program written in C-language which takes a user id & password data and save it to excel sheet. and at login it verify the credentials .
after login it takes an comment and as an output it append comment into HTML file comments.

to run use c-project.exe on system.